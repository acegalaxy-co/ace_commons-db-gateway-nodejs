# db-gateway/policies/authz.yaml
# Per-service ACL — service X được read/write table nào.
# Phase 1 — mẫu; Phase 2 enforce.

services:
  # example:
  # nexus-invoice-collector:
  #   scope: nexus
  #   postgres:
  #     read: [invoices, suppliers]
  #     write: [invoices]
  #   notion:
  #     read: ["*"]
  #     archive: ["*"]
  #     write: [invoices]

  "*":
    # Default-deny for unknown services. Must declare explicit entry.
    allow: false
